% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_protection_coverage.R
\name{assess_protection_coverage}
\alias{assess_protection_coverage}
\title{Assess protection coverage}
\usage{
assess_protection_coverage(
  data,
  areas,
  indicator_var_name,
  areaID,
  scoring,
  indicator = NULL,
  type = NULL,
  units = NULL,
  PPTID = NULL,
  project_short_title = NULL,
  climate = NULL,
  design_target = NULL,
  other_nest_variables = NULL
)
}
\arguments{
\item{data}{An \code{sf} object containing the spatial features to be assessed.
The data must contain the column specified by \code{indicator_var_name}, as
well as \code{min_target}, \code{max_target}, and \code{plainname} columns.}

\item{areas}{An \code{sf} object containing the conservation area boundaries.}

\item{indicator_var_name}{Character string identifying the column containing
the indicator or feature being assessed.}

\item{areaID}{Character string identifying the column in \code{areas} that
identifies each conservation area.}

\item{scoring}{Character string specifying the protection coverage scoring
method. Must be either \code{"protection coverage"} or
\code{"protection coverage: weighted"}.}

\item{indicator}{Optional character string identifying the indicator.}

\item{type}{Optional character string describing the indicator type.}

\item{units}{Optional character string specifying the units of the
indicator.}

\item{PPTID}{Optional identifier for the associated PPT. Multiple values
are collapsed using \code{" ;;; "}.}

\item{project_short_title}{Optional short title of the project. Multiple
values are collapsed using \code{" ;;; "}.}

\item{climate}{Optional character string describing the climate context of
the indicator.}

\item{design_target}{Optional design target associated with the indicator.}

\item{other_nest_variables}{Optional character vector containing additional
variables. For \code{"protection coverage: weighted"}, the first variable is
used as the weighting variable.}
}
\value{
A data frame containing one row per conservation area, with the
following columns:
\describe{
\item{areaID}{Identifier for the conservation area.}
\item{data}{Nested spatial data representing the indicator features
intersecting the conservation area.}
\item{coverage}{Proportion of the indicator protected within the
conservation area.}
\item{score}{Protection coverage score from 0 to 100.}
\item{indicator}{Indicator associated with the assessment.}
\item{type}{Indicator type.}
\item{units}{Indicator units.}
\item{scoring}{Protection coverage scoring method used.}
\item{PPTID}{Associated PPT identifier.}
\item{project_short_title}{Project short title.}
\item{climate}{Climate context associated with the indicator.}
\item{design_target}{Design target associated with the indicator.}
\item{status_statement}{Statement describing the observed protection
coverage and established protection target.}
\item{trend_statement}{Statement indicating that the assessment has no
temporal dimension.}
}
}
\description{
Calculates the proportion of an indicator feature that occurs within
conservation areas and converts the resulting protection coverage into a
score based on predefined minimum and maximum protection targets.
}
\details{
Two scoring approaches are supported. \code{"protection coverage"} calculates
coverage based on the proportion of each feature's area that intersects a
conservation area. \code{"protection coverage: weighted"} additionally weights
coverage according to a variable supplied in \code{other_nest_variables}.

Scores are based on the relationship between observed protection coverage
and the indicator's \code{min_target} and \code{max_target}. Coverage between the two
targets receives a score of 100. Coverage below the minimum target receives
a proportional score, while coverage above the maximum target is penalized
according to the amount by which it exceeds the maximum target.

The input geometries are transformed to the coordinate reference system of
\code{areas} and made valid before spatial intersection. Features with missing
indicator values are excluded.

For the standard \code{"protection coverage"} method, the area of each input
feature is calculated before intersection with conservation areas. The
proportion of each feature occurring within a conservation area is then
calculated as:

\deqn{
coverage = \frac{covered\ area}{total\ feature\ area}
}

For \code{"protection coverage: weighted"}, the first variable specified in
\code{other_nest_variables} is used as a weight. The contribution of each
feature to protection coverage is calculated from its weight and the
proportion of its area that is protected.

Conservation areas with no intersecting indicator features are retained in
the output and assigned a coverage of zero.

The protection coverage score is calculated using the minimum and maximum
protection targets:

\itemize{
\item{Below \code{min_target}: the score increases proportionally with
coverage.}
\item{Between \code{min_target} and \code{max_target}: the score is 100.}
\item{Above \code{max_target}: the score decreases as coverage exceeds the
maximum target.}
}

The function assumes that \code{min_target} and \code{max_target} are proportions
between 0 and 1.
}
\examples{
\dontrun{
# Standard protection coverage
result <- assess_protection_coverage(
  data = habitat_data,
  areas = MPAs,
  indicator_var_name = "habitat_type",
  areaID = "NAME_E",
  scoring = "protection coverage",
  indicator = "Habitat protection",
  type = "habitat",
  units = "proportion"
)

# Weighted protection coverage
result <- assess_protection_coverage(
  data = habitat_data,
  areas = MPAs,
  indicator_var_name = "habitat_type",
  areaID = "NAME_E",
  scoring = "protection coverage: weighted",
  other_nest_variables = "habitat_weight",
  indicator = "Weighted habitat protection",
  type = "habitat",
  units = "proportion"
)
}

}
